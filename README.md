# mel-thomas.github.io
my reading log 🪵

# make one yourself
1. `git clone https://github.com/mel-thomas/mel-thomas.github.io.git`
2. `npm install -g json-server`
3. `npm start`
4. Sub in your own reading data in `database/db.json`
